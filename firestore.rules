rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Any signed-in user can read and write — keeps all features working
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
